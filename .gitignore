/build
/bin
/play
/local-config.properties
/local-boot.properties
/build.properties
*~
out
/resources/compiled
/resources/classes
/resources/custom/
/nbproject
/accounts.json
/config.json
/error.log
/i10n/**/missing/
/windows.json
/charterbook.json
/autochoose.json
/.bin2

# Claude Code: per-user local settings (skills/ tree is shared and tracked)
.claude/settings.local.json

# One-off scratch audit notes; superseded by per-merge commit messages
# and the docs/upstream-sync.md log
/merge-audit.md

# LayerUtil decoded resource trees (generated by `LayerUtil -d` / `-rd`).
# Per CLAUDE.md: `dout/` is the decode output dir; `nres/` and `ntest/`
# are working areas for resource experiments. None are source of truth;
# regenerated from .res files on demand.
/resources/decoded/
/resources/dout/nres/
/resources/nres/
/resources/ntest/